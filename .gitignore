node_modules
out
.vscode-test
*.vsix
.beads
.beads/
# Explicitly ignore claim lock created at runtime
.beads/claim.lock
.beads/merge.lock
DISTRIBUTION.md
AGENTS.md
.gitattributes

# Local temporary files (DO NOT use /tmp/ - use this local tmp/ instead)
tmp/

# Git worktrees for multi-agent workflow (stored outside repo)
# Note: worktrees are at ../worktrees/, not in repo, but just in case
worktrees/
