import { ScenarioSpec } from './types';

export const graphBasic: ScenarioSpec = {
  id: 'graph-basic',
  title: 'Dependency graph baseline',
  description: 'Show graph view with mixed dependency types and a missing node.',
  keys: [],
  initialTab: 'graph',
  width: 80,
  height: 24,
};

export default graphBasic;
